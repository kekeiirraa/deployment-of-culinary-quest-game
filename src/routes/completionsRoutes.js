// Completions routes are now nested under /challenges/:challenge_id/completions
// This file is kept for backward compatibility but routes are in challengesRoutes.js
const express = require('express');
const router = express.Router();

module.exports = router;
